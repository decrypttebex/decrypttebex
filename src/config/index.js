const fs=require('fs'),path=require('path');const {z}=require('zod');const file=path.resolve('config.json'),c=fs.existsSync(file)?JSON.parse(fs.readFileSync(file,'utf8')):{};const list=v=>String(v||'').split(',').filter(Boolean),baseUrl=process.env.BASE_URL||c.baseUrl||(process.env.VERCEL_PROJECT_PRODUCTION_URL?`https://${process.env.VERCEL_PROJECT_PRODUCTION_URL}`:'http://localhost:3000');const raw={port:Number(process.env.PORT||c.port||3000),baseUrl,databaseUrl:process.env.DATABASE_URL||c.databaseUrl,sessionSecret:process.env.SESSION_SECRET||c.sessionSecret,corsOrigins:process.env.CORS_ORIGINS?list(process.env.CORS_ORIGINS):(c.corsOrigins||[baseUrl]),adminDiscordIds:process.env.ADMIN_DISCORD_IDS?list(process.env.ADMIN_DISCORD_IDS):(c.adminDiscordIds||[]),discord:{clientId:process.env.DISCORD_CLIENT_ID||c.discord?.clientId||'',clientSecret:process.env.DISCORD_CLIENT_SECRET||c.discord?.clientSecret||'',callbackUrl:process.env.DISCORD_CALLBACK_URL||c.discord?.callbackUrl||`${baseUrl}/auth/discord/callback`},mercadoPago:{accessToken:process.env.MERCADOPAGO_ACCESS_TOKEN||c.mercadoPago?.accessToken||'',webhookSecret:process.env.MERCADOPAGO_WEBHOOK_SECRET||c.mercadoPago?.webhookSecret||'',sandbox:String(process.env.MERCADOPAGO_SANDBOX??c.mercadoPago?.sandbox??true)==='true'},upload:c.upload||{maxBytes:4000000,allowedExtensions:['.zip'],retentionDays:1},downloads:c.downloads||{ttlMinutes:30},trustProxy:process.env.VERCEL==='1'||c.trustProxy===true};const schema=z.object({port:z.number().positive(),baseUrl:z.string().url(),databaseUrl:z.string().min(1),sessionSecret:z.string().min(32),corsOrigins:z.array(z.string()),adminDiscordIds:z.array(z.string()),discord:z.object({clientId:z.string(),clientSecret:z.string(),callbackUrl:z.string().url()}),mercadoPago:z.object({accessToken:z.string(),webhookSecret:z.string(),sandbox:z.boolean()}),upload:z.any(),downloads:z.any(),trustProxy:z.boolean()});const config=schema.parse(raw);process.env.DATABASE_URL=config.databaseUrl;module.exports=config;
